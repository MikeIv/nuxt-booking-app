# Структура тестов

## Организация

Тесты организованы в централизованной структуре для лучшей поддержки и масштабируемости:

```
tests/
├── components/          # Unit тесты компонентов (Vitest)
│   └── booking/        # Тесты компонентов booking
│       └── Booking.spec.ts
├── e2e/                # E2E тесты (Playwright)
│   ├── booking/        # Тесты функциональности бронирования
│   │   └── booking.spec.ts
│   ├── fixtures/       # Тестовые данные
│   │   └── users.ts
│   └── utils/          # Утилиты для E2E тестов
│       └── test-helpers.ts
├── mocks/              # Общие моки для unit тестов
│   ├── nuxt.ts        # Моки для Nuxt composables
│   ├── stores.ts      # Моки для Pinia stores
│   └── components.ts  # Моки для компонентов
├── utils/              # Утилиты для unit тестирования
│   └── test-utils.ts  # Вспомогательные функции
└── setup.ts           # Глобальная настройка unit тестов
```

## Преимущества такой структуры

### 1. Централизация
- Все тесты в одном месте (`tests/`)
- Легко найти и поддерживать
- Не засоряет директории с исходным кодом

### 2. Переиспользование
- Общие моки в `tests/mocks/`
- Утилиты в `tests/utils/`
- Единая настройка в `tests/setup.ts`

### 3. Масштабируемость
- Легко добавлять новые тесты
- Структура повторяет структуру компонентов
- Поддержка unit тестов (Vitest) и e2e тестов (Playwright)

## Использование

### Создание нового теста компонента

1. Создайте файл в соответствующей директории:
```typescript
// tests/components/booking/NewComponent.spec.ts
import { describe, it, expect } from "vitest";
import { mountComponent } from "../../utils/test-utils";
import NewComponent from "~/components/booking/NewComponent.vue";

describe("NewComponent.vue", () => {
  it("должен отображаться", () => {
    const wrapper = mountComponent(NewComponent);
    expect(wrapper.exists()).toBe(true);
  });
});
```

### Использование утилит

```typescript
import { setupPinia, mountComponent } from "../../utils/test-utils";
import { mockRouterPush, mockToastAdd } from "../../mocks/nuxt";
import { createMockBookingStore } from "../../mocks/stores";

// Настройка Pinia
setupPinia();

// Монтирование компонента
const wrapper = mountComponent(Component);

// Использование моков
const mockStore = createMockBookingStore();
```

## Конфигурация

Конфигурация Vitest находится в `vitest.config.ts`:
- Автоматическая настройка через `tests/setup.ts`
- Поддержка алиасов (`~`, `@`)
- Настройка coverage

## Типы тестов

### Unit тесты (Vitest)
Тестируют отдельные компоненты в изоляции.

### E2E тесты (Playwright)
Тестируют полные пользовательские сценарии в реальном браузере.

## Запуск тестов

### Unit тесты

```bash
# Все unit тесты (результаты в терминале)
yarn test

# Watch режим (автоперезапуск при изменениях)
yarn test:watch

# Конкретный файл
yarn test tests/components/booking/Booking.spec.ts

# С coverage (покрытие кода)
yarn test:coverage

# UI режим (веб-интерфейс в браузере)
yarn test:ui
```

### E2E тесты

```bash
# Все e2e тесты
yarn test:e2e

# С UI интерфейсом
yarn test:e2e:ui

# В видимом режиме (headed)
yarn test:e2e:headed

# В режиме отладки
yarn test:e2e:debug

# Просмотр отчета
yarn test:e2e:report

# Конкретный файл
yarn playwright test tests/e2e/booking/booking.spec.ts
```

📖 Подробнее о E2E тестах: [tests/e2e/README.md](./e2e/README.md)

## Просмотр результатов

### 1. В терминале
При запуске `yarn test` результаты отображаются прямо в консоли:
- ✅ Пройденные тесты
- ❌ Упавшие тесты с деталями ошибок
- ⏱️ Время выполнения

### 2. В браузере (Vitest UI)
Запустите `yarn test:ui` - откроется веб-интерфейс с:
- Интерактивным списком всех тестов
- Фильтрацией и поиском
- Детальной информацией об ошибках
- Возможностью запускать отдельные тесты
- Автоматическим обновлением в watch-режиме

### 3. Coverage отчеты
После `yarn test:coverage`:
- **В терминале**: краткая сводка покрытия
- **HTML отчет**: откройте `coverage/index.html` в браузере для детального просмотра
- **JSON отчет**: `coverage/coverage-final.json` (для CI/CD)

## Best Practices

### Unit тесты
1. **Используйте утилиты** - не дублируйте код, используйте `mountComponent` и другие утилиты
2. **Переиспользуйте моки** - создавайте моки в `tests/mocks/` для общих зависимостей
3. **Следуйте структуре** - тесты должны повторять структуру компонентов
4. **Изолируйте тесты** - каждый тест должен быть независимым
5. **Используйте описательные имена** - тесты должны быть самодокументируемыми

### E2E тесты
1. **Используйте утилиты** - не дублируйте код, используйте функции из `tests/e2e/utils/test-helpers.ts`
2. **Мокируйте API** - используйте `page.route()` для мокирования запросов
3. **Используйте data-testid** - добавляйте `data-testid` атрибуты для более стабильных селекторов
4. **Ожидайте элементы** - всегда используйте `waitFor` или `expect().toBeVisible()`
5. **Обрабатывайте асинхронность** - используйте `await` для всех асинхронных операций

